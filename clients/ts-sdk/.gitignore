node_modules/
dist/
*.tsbuildinfo
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json is tracked (reproducible CI + release installs via
# `npm ci`). Alternative package managers' lockfiles stay ignored.
yarn.lock
pnpm-lock.yaml

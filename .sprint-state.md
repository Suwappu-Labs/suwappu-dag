# Sprint state — bootstrap + DAG-S1

**Branch:** `main`
**Started:** 2026-05-12
**Exit gates:**

- Bootstrap: `cargo build --workspace` green, 10 crates compile.
- DAG-S1: 7 properties × 10k cases under `PROPTEST_CASES=10000 cargo test --release`.

## Completed

- [x] Workspace skeleton with 10 paper-mapped crates (`gsx-crypto`,
  `gsx-consensus`, `gsx-authority`, `gsx-validator`, `gsx-fastpath`,
  `gsx-execution`, `gsx-precompiles`, `gsx-ltp`, `gsx-transport`, `gsx-node`).
- [x] `gsx-crypto`: ML-DSA-65, ML-KEM-768, BLS12-381 (single + aggregate),
  SHA3-256 (length-prefixed domain separation).
- [x] 22 unit tests (FIPS 202 KATs for SHA3-256, key wrong-flow rejections,
  BLS 7-of-9 aggregate matching LTP paper quorum).
- [x] **DAG-S1 EXIT GATE: ✅ MET.** 7 properties × 10,000 cases = 70,000 cases
  passing in 41 s release-mode under `cargo test -p gsx-crypto --release`.
- [x] `Cargo.toml` workspace dependencies fixed at NIST-standard parameter
  sets (`pqcrypto-mldsa = 0.1`, `pqcrypto-mlkem = 0.1`, `blst = 0.3`).
- [x] `deny.toml` license allowlist + unknown-registry deny.
- [x] `rustfmt.toml` mirrors `gsx-db`.

## DAG-S1 EXIT GATE: ✅ MET

```
running 7 tests
test sha3_domain_separation_is_meaningful ... ok
test mlkem_recovers_shared_secret ... ok
test sha3_is_deterministic ... ok
test mldsa_rejects_flipped_message ... ok
test mldsa_signs_what_it_verifies ... ok
test bls_signs_what_it_verifies ... ok
test bls_aggregate_verifies ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 40.89s
```

## Pending in this branch

- [ ] CLAUDE.md ✅ landed
- [ ] `docs/architecture/` from v7 paper §3–§14
- [ ] `terraform/` skeleton targeting gsn profile (account 492042618949)
- [ ] `.github/workflows/ci.yml`
- [ ] Push to `GlobalSettlementNetwork/gsx-dag` `main`

## Carried forward

- NIST ACVP vector integration for ML-DSA-65 and ML-KEM-768 (DAG-S1.1, see
  the `nist-acvp-pqc-test-vectors` skill).
- Poseidon2 implementation (deferred; not used on LTP commitment surface).
- BLS12-381 hash-based + SP1-STARK aggregation migration (DAG-S15+).

## Blockers

None for the current branch.

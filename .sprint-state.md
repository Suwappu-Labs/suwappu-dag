# Gap-closure loop — progress ledger

**Branch:** `claude/chain-research-gap-analysis-5hoppa`
**Driving doc:** `docs/research/competitive-gap-analysis.md` §6
**Goal:** every workstream implemented (committed, pushed, CI-validated) or
explicitly dispositioned as blocked-on-human with a handoff note.
**Loop started:** 2026-07-03
**Supersedes:** the DAG-S31 handoff previously in this file (see git
history at `52ae9cf^` for the full text; still-relevant notes carried
into "Operational notes" below).

## Status key

⬜ not started · 🟡 in progress · 🔶 pushed, awaiting CI · ✅ done (CI green) ·
⛔ blocked-on-human (see handoff note)

## P0 — credibility

| ID | Scope | Status | Notes |
|---|---|---|---|
| PERF-1 | Fix round-advance stall; re-run multi-region campaign | ✅ (code) | **CI green** (run #40, commit c048237). Follow-ups filed as **issue #17** (restart self-equivocation, 10k liveness proptest, walk-resume). Snap-up fix implemented. Consensus-reviewer round 1: **BLOCK** — the perf report's `max(last, observed)+1` prescription is off by one (abandons the frontier round → cluster-wide propose-gate deadlock) and `max_observed_round` is Byzantine-steerable. Revised: snap **to** frontier (`max(last+1, observed)`) + density clamp (walk only through rounds with ≥ f+1 authors) + per-target-round leader timer + staggered-tick 4-node liveness test + perf-report correction note. Round 2: **APPROVE-WITH-NITS** (nits fixed: saturation doc caveat, test port base). Follow-ups to file as issues: restart resets `last_authored_round` → potential self-equivocation on reboot (pre-existing); 10k round-driver liveness proptest; walk-resume optimization. **AWS re-run is ⛔ blocked-on-human** — handoff note below. |
| PERF-2 | Fast-path latency measurement harness | 🔶 | Harness committed (1dd80ca): client-wire SubmitFastPath/GetLineage, `--fastpath` loadgen, `--mode fastpath` metrics, DEVNET section. fastpath-auditor round 1 **BLOCK** (exposing `propose_fastpath_tx` on the public port → Invariant-5 vectors) → all fixes applied (pin/main-lane/lineage guards in propose, owner binding, intersection slash attribution, saturating_add, O(1) committed-head scalar, MAX_FASTPATH_TRACKED cap, 3 regression tests) → round 2 **APPROVE-WITH-NITS**. Residual (post-commit under-slash + per-signer cert sigs) is observability-only today and gated on the future slashing-enablement sprint — filed as **issue #16**. CI #41 in progress. Also fixed pre-existing red main CI (rustc 1.96 clippy/fmt + anyhow RUSTSEC-2026-0190), CI #40 green. |
| LAUNCH-1 | Close ROADMAP G2/G3/G6/G7/G8 | ⬜ | G2 + infra legs need AWS — expect partial ⛔; explorer (G7)/status (G8) SPA + docs legs doable in-repo. |

## P1 — table stakes

| ID | Scope | Status | Notes |
|---|---|---|---|
| FEE-1 | IQ: fee-payer/sender separation + sponsorship; stablecoin-fee decision | ⬜ | IQ first per workflow. |
| BRIDGE-1 | Wire submitHeader → quorum oracle → mint end-to-end on public EVM testnet | ⬜ | Destination contracts live in suwappu-revm / suwappu-db — likely partial ⛔ (cross-repo + deploy). Source-side + relayer + runbook doable here. |
| EVM-1 | IQ: `eth_*` RPC compat layer vs Intent-SDK-only | ⬜ | IQ first. |
| CONF-1 | Track H confidential-balance demo w/ auditor disclosure | ⬜ | |

## P2 — differentiation defense

| ID | Scope | Status | Notes |
|---|---|---|---|
| PQ-1 | IQ: LTP BLS12-381 aggregate migration target + timeline | ⬜ | IQ first. |
| COMP-1 | GENIUS/MiCA/TFR ↔ precompile mapping doc | ⬜ | |
| INTEROP-1 | IQ: OFT-class adapter feasibility | ⬜ | IQ first. |
| GTM-1 | Institutional design-partner outreach kit | ⬜ | Draft doc from P0/P1 proof points, then ⛔ for human GTM execution. |

## Handoff notes (blocked-on-human)

### PERF-1 — AWS multi-region re-run

The code half (round-advance snap-up) is implemented on this branch. The
measurement half needs the AWS campaign re-run (4-region EC2 + CodeBuild +
loadgen per `docs/perf-run-2026-05-13/README.md`), which requires the
`gsn` AWS profile and asked-tier deploy scripts — not available in this
environment. When the fix lands on main: redeploy via SSM and re-run the
2026-05-13 campaign unchanged; publish `docs/perf-run-<date>/` with
**committed TPS** as the headline metric. Note the later S30 campaign
(2026-05-14) measured commit cadence stuck at ~0.125 TPS sustained with
ap-northeast-1 buried under inbound storm — the snap-up fix targets the
round-stall half; the inbox-storm half was S31 scope (per-peer inbox
tasks, state split — S31.2 landed).

## Operational notes (carried from S31 handoff)

- **No local `cargo test`/`build`/`clippy` `--workspace`** — CI (GHA) is
  the iteration loop. Also true in the remote sandbox: the `suwappu-db`
  git dependency is not fetchable here, so even `cargo check -p` fails;
  push and let CI validate.
- Perf validators (if still allocated): us-east-1 `i-09ec6b896639cadac`,
  us-west-2 `i-06c0063e7fd26a14e`, eu-west-1 `i-08d7f82fd9b70bf48`,
  ap-northeast-1 `i-046500481231aa287`; client port 9091, consensus 9090;
  SSM redeploy pattern pulls binary + node.toml from S3.
- One final campaign per change-batch, not per commit (user preference).

## Log

- 2026-07-03: Loop started. Ledger created (supersedes stale S31
  handoff). PERF-1 code fix implemented (`next_target_round` snap-up +
  unit test in `crates/suwappu-node/src/daemon.rs`); consensus-reviewer
  subagent dispatched; push pending its verdict.
- 2026-07-03 (later): consensus-reviewer BLOCKed rev 1 (off-by-one in
  the perf report's own prescription + Byzantine-steerable snap
  target); rev 2 (frontier snap + density clamp + per-round timer +
  staggered-tick test) APPROVE-WITH-NITS; nits fixed; pushed. CI is
  the compile/test gate (cargo unavailable in sandbox).
- 2026-07-03 (later): first CI dispatch (#39) failed on THREE
  pre-existing main-branch breakages (rustc 1.96 clippy unnecessary_sort_by
  + rustfmt reflow in suwappu-execution; anyhow RUSTSEC-2026-0190). Fixed
  in commit c048237; CI #40 green → PERF-1 validated.
- 2026-07-03 (later): PERF-2 harness built by subagent; fastpath-auditor
  BLOCKed (Invariant-5 equivocation vectors from exposing propose on the
  public port). Fixes dispatched back to the builder; re-review pending.

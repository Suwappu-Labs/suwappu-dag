[package]
name = "gsx-execution"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "DAG block executor — wires the gsx-db state substrate into the consensus pipeline (paper §7)."

[dependencies]
gsx-authority.workspace = true
gsx-consensus.workspace = true
gsx-crypto.workspace = true
gsx-l2-verifier-precompile.workspace = true
gsxdb-bridge.workspace = true
gsxdb-state.workspace = true
blake3.workspace = true
hex.workspace = true
serde.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
proptest.workspace = true
rand.workspace = true
# Pins the canonical bincode discriminant of each `Intent` variant
# (IQ-007, #241). Must match the codec the mempool/rpc_adapter hash
# path uses: `bincode::config::legacy()` (fixint, little-endian).
bincode = { workspace = true }
